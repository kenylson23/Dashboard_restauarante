{
  "version": 2,
  "buildCommand": "cd client && NODE_ENV=production npx vite build --outDir ../dist/public",
  "outputDirectory": "dist/public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}