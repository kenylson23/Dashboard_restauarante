{
  "version": 2,
  "buildCommand": "NODE_ENV=production npx vite build --config vite.config.prod.ts",
  "outputDirectory": "dist/public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}